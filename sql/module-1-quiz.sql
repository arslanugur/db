      to list the table names.
SHOW TABLES;


      Why use primary keys? To guarantee the uniqueness of a row

      to select distinct names from the ''students'' table, ordered by name.
SELECT DISTINCT name 
FROM students
ORDER BY name;  
